
export enum LimitOfferType {
    ACCOUNT_LIMIT = "ACCOUNT_LIMIT",
     PER_TRANSACTION_LIMIT = "PER_TRANSACTION_LIMIT"
};

export enum OfferStatus {
    PENDING = "PENDING",
    ACCEPTED = "ACCEPTED", REJECTED  = "REJECTED"
}